namespace BikeTDD.Tests;

public class CustomerDetailsServiceTests
{
  
}

//1. Create a Customer Account (via form)
//input takes name, birthdate, phone, email, address
//generates Unique ID- var guid = Guid.NewGuid()ToString(;
//test guid for type Assert.IsInstanceOfType(method(), typeof(Guid));
//save fav locals